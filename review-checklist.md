# 원문 대조 검수 체크리스트

`pages/partN*.pdf` 원본과 `pages/*_타임라인_v1.html` 업무카드를 대조해
① 원문과 다른 서술, ② 원문에 없는 창작, ③ 원문에 있는데 빠진 내용을 찾아 고친다.
검수 과정에서 내린 판단과 그 근거는 `review-notes.md`에 남긴다.

## 진행 현황

- [x] Part 1 학교업무분장 · Section_01~08 · pp.3–62 · 카드 29 → 30
- [ ] Part 2 교육과정 · 8편
  - [x] Section_01 입학생 학교교육과정 관리 · pp.65–77 · 카드 9(변동 없음)
  - [ ] Section_02 (건너뜀 · 나중에)
  - [x] Section_03 학급담임교사 편성 · pp.87–90 · 카드 6(변동 없음)
  - [x] Section_04 학교교육과정 편성 · pp.91–119 · 카드 11(변동 없음)
  - [x] Section_05 과목개설 관리 · pp.120–150 · 카드 21 + 참고카드 1
  - [ ] Section_06 (건너뜀 · 나중에)
  - [x] Section_07 시간표 관리 · pp.162–195 · 카드 17(변동 없음)
  - [x] Section_08~09 시간표 및 수업 조회 · 학교일지 관리 · pp.196–205 · 카드 10(변동 없음)
  - [ ] 남은 것 · Section_02, Section_06
- [ ] Part 3 학적 · 19편
- [ ] Part 4 개별화교육계획 · 1편
- [ ] Part 5 학생생활 · 11편
- [ ] Part 6 성적 · 8편
  - [x] Section_01 학기초 성적처리 선행작업 · pp.731–752 · 카드 13(변동 없음)
  - [x] Section_02 1·2차 정기시험 성적처리 · pp.753–795 · 카드 23(변동 없음)
  - [x] Section_03 수행평가 성적처리 · pp.796–815 · 카드 12(변동 없음)
  - [x] Section_04 추정분할점수 산출 · pp.816–829 · 카드 13(변동 없음)
  - [x] Section_05 학기말 성적처리 · pp.830–894 · 카드 29(변동 없음)
  - [x] Section_06 성적 조회 및 통계 · pp.895–921 · 카드 14(변동 없음)
  - [ ] Section_07~08
- [ ] Part 7 학생부 · 2편
- [ ] Part 8 보건 · 2편
- [ ] Part 9 입학 · 1편

## Part별 공통 절차

1. **원문 쪽별 텍스트 추출** → `pdftotext -layout -f N -l N` 로 쪽마다 잘라 뽑고,
   본문 하단의 `◀◀◀ N` / `N ▶▶▶` 로 인쇄 쪽번호를 확인한다.
   Part 1은 물리 쪽 = 인쇄 쪽 + 0 이었으나 Part마다 다를 수 있으므로 매번 확인한다.
2. **카드 텍스트 추출** → BeautifulSoup으로 `article.task` 를 뽑아
   `data-num`, `data-who`, `h3`, `p.src`, `div.w6`, `div.memo` 를 평문으로 편다.
3. **기계 대조** → 아래 세 가지는 스크립트로 돌린다. 눈으로 읽지 않는다.
   - 대괄호 메뉴경로 집합 차집합(양방향)
   - 중괄호 버튼명 집합 차집합(양방향)
   - 수치 표현(개월·일·년·가지·개·세대 등) 빈도 비교
4. **정독 대조** → 카드마다 원문 해당 쪽을 열어 놓고 여섯 항목을 본다.
5. **표·그림 확인** → `pdftoppm -jpeg -r 160 -f N -l N` 로 그 쪽을 그림으로 떠서
   본다. 표가 이미지면 텍스트 추출로는 잡히지 않으므로 반드시 눈으로 확인한다.

## 카드 한 장을 볼 때 확인할 것

- [ ] `data-who` 와 '누가?' 항목이 같은 사람을 가리키는가
- [ ] '언제?' 가 원문 Section의 `￭ 업무시기` 와 어긋나지 않는가
- [ ] '어디서?' 의 메뉴경로가 원문 표기와 글자까지 같은가
- [ ] '무엇을 어떻게?' 의 절차 순서와 버튼명이 원문 ➊➋➌ 순서와 같은가
- [ ] '왜?' 가 원문 근거가 있는 문장인가, 저술된 해석인가
- [ ] `p.src` 의 쪽 표기가 카드가 실제로 담은 내용의 쪽을 모두 덮는가.
      **여러 메뉴를 한 카드로 묶었다면 묶인 메뉴가 있는 쪽을 모두 적었는지** 특히 확인한다
- [ ] 버튼명이 모두 `<span class="btn">` 으로 감싸였는가(맨 텍스트로 남은 것이 없는가).
      단, SVG `<text>` 안의 버튼 표기와 **각주(`.fnote`)가 인용한 원문 표기**는 검사에서
      제외한다. 각주가 인용하는 잘못된 표기는 `.wrong` 으로 두어 버튼처럼 보이지 않게 한다
- [ ] 원문에 없는 인과 표현(`~해야 ~할 수 있음`, `~하지 않으면 ~불가`)을 새로 만들지 않았는가
- [ ] 원문에 없는 배타 표현(`~만`, `반드시`)을 덧붙이지 않았는가
- [ ] **같은 Part 안에서 두 쪽이 서로 다르게 적은 내용을 카드가 각각 따로 옮겨, 카드끼리 모순되지 않는가**
- [ ] **같은 메뉴를 다른 Section도 설명하고 있지 않은가.** 설명이 갈리면 어느 쪽이 더 완전한지
      확인하고, 부족한 쪽 카드를 보강할지 `review-notes.md`에 남긴다
- [ ] 원문에 정의가 없는 낱말을 임의로 풀어 쓰지 않았는가

## Part 문서 한 편을 마칠 때 확인할 것

- [ ] 원문 Section의 하위 절차(`1.`, `2.`, `1)`, `2)`)마다 대응 카드가 있는가
- [ ] 원문의 `￭ 알아두기` 항목이 어느 카드의 실무 메모에든 들어갔는가
- [ ] 원문의 Q&A 말풍선이 어느 카드에든 들어갔는가
- [ ] 원문의 표는 표로 옮겼는가(줄글 요약으로 눌러 담지 않았는가). 옮기지 않았다면
      그 사실이 `review-notes.md`에 기록되었는가
- [ ] 표를 줄글로 요약했다면 원문 표의 **모든 열**이 살아 있는가(한 열이 통째로
      사라지지 않았는가)
- [ ] 어느 카드에도 인용되지 않은 원문 쪽이 남았는가
- [ ] 원문의 예시 연도(`2025학년도` 등)를 자리표시자로 바꿨다면, 같은 화면 문구를 쓰는
      다른 카드에도 같은 표기를 적용했는가

## 용어를 일괄로 바꿀 때

학교에서 쓰는 말로 용어를 바꿔 달라는 요청이 오면(예: 지필평가 → 정기시험),
어디까지 바꿀지 먼저 확인한다. 서술 문장만 바꿀 수도 있고, 나이스 화면에 찍히는
메뉴경로·창 이름까지 바꿀 수도 있다. **Part 6은 화면 이름까지 모두 바꾸기로 했다.**

- [ ] **`href`·`src` 등 속성의 파일명은 절대 바꾸지 않는다.** 실제 파일 이름이라
      바꾸면 링크가 깨진다. 교체 전에 속성값을 잠그고 본문만 바꾼다
- [ ] 바꾼 뒤 저장소 전체 링크 무결성 검사를 돌린다
- [ ] **조사를 확인한다.** 받침 유무가 달라지면(지필평가→정기시험) 를/을, 가/이,
      는/은, 와/과, 로/으로가 어긋난다. **닫는 따옴표 뒤에 붙은 조사**(`'정기시험'로`)도
      함께 잡는다. 검사 정규식을 회귀 테스트에 넣는다
- [ ] **타임라인과 히어로는 `<main>` 밖에 있다.** `<main>` 안만 훑으면 빠진다
- [ ] 이웃 문서 링크는 **이름만** 바꾸고 `href`는 그대로 둔다. 그 문서를 아직 안 바꿨다면
      두 문서의 표기가 갈린다는 사실을 기록한다
- [ ] 바꾼 사실을 `review-notes.md`에 적는다. 이후 원문 대조에서 메뉴경로 차집합이
      잡히더라도 오류가 아니다
- [ ] 색인 검색에 영향이 있다. 옛 용어로 검색하면 바뀐 문서의 카드는 나오지 않는다
- [ ] **문서 제목에 옛 용어가 들어 있으면** 파일명은 그대로 두고 `manifest.js` 항목에
      `label: '새 제목'` 을 넣는다. 색인(`cards.js`)과 허브(`index.html`)가 그 제목을 쓴다.
      문서 안의 `<title>`·`h1`·`.eyebrow` 와 이웃 문서의 링크 이름도 함께 바꾼다
- [ ] 바꾼 뒤 `manifest.js` 가 자바스크립트로 파싱되는지 확인한다

## 참고카드를 넣을 때

절차가 아니라 여러 Section을 잇는 설명 카드는 업무카드와 구분한다.

- [ ] 6하원칙 블록(`div.w6`)을 두지 않았는가
- [ ] `data-stage="참고"` 와 '참고 · 절차 아님' 태그로 표시했는가
- [ ] **각주는 절차 문장 한가운데가 아니라 실무 메모 항목으로 붙인다.** 절차 중간에 끼면
      읽기 흐름이 끊긴다
- [ ] 그림으로 주장하는 관계가 원문의 `￭ 선행처리`·`￭ 후행처리`나 본문에 근거가 있는가.
      근거가 있으면 카드 안에 원문 쪽수를 밝힌다
- [ ] 다른 Section의 버튼명을 인용했다면, 그 사실을 `review-notes.md`에 적어
      버튼명 차집합 대조의 예외로 남겼는가
- [ ] SVG마다 `role="img"` 와 `aria-label` 을 달았는가
- [ ] 회귀 테스트가 `div.w6` 유무로 업무카드와 참고카드를 나누는가

## 카드를 고치거나 늘린 뒤

- [ ] 히어로 `<em>업무 N개를…</em>` 의 개수를 맞췄는가
- [ ] 타임라인 `.tl-btns` 에 새 카드 버튼을 넣었는가
- [ ] `README.md` 의 해당 편 업무카드 개수를 고쳤는가
- [ ] `python3 tools/build_cards.py` 를 다시 돌렸는가
- [ ] 문서를 브라우저로 열어 카드 이동·검색·원문 패널이 동작하는가
- [ ] 저술 문장(`p.desc`, `왜?`)의 문체가 문서 안에서 갈리지 않는가.
      원문을 옮긴 절차·실무 메모의 개조식(`~함`, `~음`)은 원문 표기이므로 바꾸지 않는다
