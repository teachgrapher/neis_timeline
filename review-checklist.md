# 원문 대조 검수 체크리스트

`pages/partN*.pdf` 원본과 `pages/*_타임라인_v1.html` 업무카드를 대조해
① 원문과 다른 서술, ② 원문에 없는 창작, ③ 원문에 있는데 빠진 내용을 찾아 고친다.
검수 과정에서 내린 판단과 그 근거는 `review-notes.md`에 남긴다.

## 진행 현황

- [x] Part 1 학교업무분장 · Section_01~08 · pp.3–62 · 카드 29 → 30
- [ ] Part 2 교육과정 · 8편
  - [x] Section_01 입학생 학교교육과정 관리 · pp.65–77 · 카드 9(변동 없음)
  - [ ] Section_02 (건너뜀 · 나중에)
  - [x] Section_03 학급담임교사 편성 · pp.87–90 · 카드 6(변동 없음)
  - [x] Section_04 학교교육과정 편성 · pp.91–119 · 카드 11(변동 없음)
  - [x] Section_05 과목개설 관리 · pp.120–150 · 카드 21(변동 없음)
  - [ ] Section_06~08
- [ ] Part 3 학적 · 19편
- [ ] Part 4 개별화교육계획 · 1편
- [ ] Part 5 학생생활 · 11편
- [ ] Part 6 성적 · 8편
- [ ] Part 7 학생부 · 2편
- [ ] Part 8 보건 · 2편
- [ ] Part 9 입학 · 1편

## Part별 공통 절차

1. **원문 쪽별 텍스트 추출** → `pdftotext -layout -f N -l N` 로 쪽마다 잘라 뽑고,
   본문 하단의 `◀◀◀ N` / `N ▶▶▶` 로 인쇄 쪽번호를 확인한다.
   Part 1은 물리 쪽 = 인쇄 쪽 + 0 이었으나 Part마다 다를 수 있으므로 매번 확인한다.
2. **카드 텍스트 추출** → BeautifulSoup으로 `article.task` 를 뽑아
   `data-num`, `data-who`, `h3`, `p.src`, `div.w6`, `div.memo` 를 평문으로 편다.
3. **기계 대조** → 아래 세 가지는 스크립트로 돌린다. 눈으로 읽지 않는다.
   - 대괄호 메뉴경로 집합 차집합(양방향)
   - 중괄호 버튼명 집합 차집합(양방향)
   - 수치 표현(개월·일·년·가지·개·세대 등) 빈도 비교
4. **정독 대조** → 카드마다 원문 해당 쪽을 열어 놓고 여섯 항목을 본다.
5. **표·그림 확인** → `pdftoppm -jpeg -r 160 -f N -l N` 로 그 쪽을 그림으로 떠서
   본다. 표가 이미지면 텍스트 추출로는 잡히지 않으므로 반드시 눈으로 확인한다.

## 카드 한 장을 볼 때 확인할 것

- [ ] `data-who` 와 '누가?' 항목이 같은 사람을 가리키는가
- [ ] '언제?' 가 원문 Section의 `￭ 업무시기` 와 어긋나지 않는가
- [ ] '어디서?' 의 메뉴경로가 원문 표기와 글자까지 같은가
- [ ] '무엇을 어떻게?' 의 절차 순서와 버튼명이 원문 ➊➋➌ 순서와 같은가
- [ ] '왜?' 가 원문 근거가 있는 문장인가, 저술된 해석인가
- [ ] `p.src` 의 쪽 표기가 카드가 실제로 담은 내용의 쪽을 모두 덮는가
- [ ] 버튼명이 모두 `<span class="btn">` 으로 감싸였는가(맨 텍스트로 남은 것이 없는가)
- [ ] 원문에 없는 인과 표현(`~해야 ~할 수 있음`, `~하지 않으면 ~불가`)을 새로 만들지 않았는가
- [ ] 원문에 없는 배타 표현(`~만`, `반드시`)을 덧붙이지 않았는가
- [ ] **같은 Part 안에서 두 쪽이 서로 다르게 적은 내용을 카드가 각각 따로 옮겨, 카드끼리 모순되지 않는가**
- [ ] 원문에 정의가 없는 낱말을 임의로 풀어 쓰지 않았는가

## Part 문서 한 편을 마칠 때 확인할 것

- [ ] 원문 Section의 하위 절차(`1.`, `2.`, `1)`, `2)`)마다 대응 카드가 있는가
- [ ] 원문의 `￭ 알아두기` 항목이 어느 카드의 실무 메모에든 들어갔는가
- [ ] 원문의 Q&A 말풍선이 어느 카드에든 들어갔는가
- [ ] 원문의 표는 표로 옮겼는가(줄글 요약으로 눌러 담지 않았는가). 옮기지 않았다면
      그 사실이 `review-notes.md`에 기록되었는가
- [ ] 표를 줄글로 요약했다면 원문 표의 **모든 열**이 살아 있는가(한 열이 통째로
      사라지지 않았는가)
- [ ] 어느 카드에도 인용되지 않은 원문 쪽이 남았는가
- [ ] 원문의 예시 연도(`2025학년도` 등)를 자리표시자로 바꿨다면, 같은 화면 문구를 쓰는
      다른 카드에도 같은 표기를 적용했는가

## 카드를 고치거나 늘린 뒤

- [ ] 히어로 `<em>업무 N개를…</em>` 의 개수를 맞췄는가
- [ ] 타임라인 `.tl-btns` 에 새 카드 버튼을 넣었는가
- [ ] `README.md` 의 해당 편 업무카드 개수를 고쳤는가
- [ ] `python3 tools/build_cards.py` 를 다시 돌렸는가
- [ ] 문서를 브라우저로 열어 카드 이동·검색·원문 패널이 동작하는가
- [ ] 저술 문장(`p.desc`, `왜?`)의 문체가 문서 안에서 갈리지 않는가.
      원문을 옮긴 절차·실무 메모의 개조식(`~함`, `~음`)은 원문 표기이므로 바꾸지 않는다
