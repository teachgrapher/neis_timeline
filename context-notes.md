# 컨텍스트 노트

## 결정 1. 빌드 단계를 두지 않는다

검색 색인을 미리 만들어 `search-index.json`으로 뽑는 방식과, 브라우저가 로드 직후 각 html을 읽어 색인하는 방식을 놓고 골랐다.
후자를 택했다. 문서를 한 편씩 늘려가는 작업 방식이라 파일을 추가할 때마다 스크립트를 돌려야 하는 부담이 반복 비용으로 크게 쌓이기 때문이다.
지금은 `manifest.js`에서 `ready: 0`을 `1`로 바꾸는 것만으로 버튼과 검색이 동시에 살아난다.

되돌릴 조건이 있다. 51편을 다 올렸을 때 첫 검색까지 눈에 띄게 느려지면(대략 3초 초과) 그때 사전 색인 방식으로 바꾼다.
문서 하나가 평균 60KB 안팎이므로 51편이면 3MB 정도이고, gzip 압축되면 실제 전송량은 그보다 훨씬 작을 것으로 본다.

## 결정 2. 파일 목록을 `manifest.js`로 뺀다

정적 호스팅은 폴더 목록을 읽을 수 없어 파일 목록이 어딘가에는 있어야 한다.
`index.html` 안에 박아 두면 목록을 고칠 때마다 본문 코드를 건드리게 되므로 별도 파일로 분리했다.
51편 전부를 미리 적어 두고 `ready` 플래그로 공개 여부만 관리한다. 남은 작업량이 첫 화면에 그대로 보이는 부수 효과도 있다.

## 결정 3. 문서 제목은 파일명에서 만든다

`나이스_학교교육과정_편성_타임라인_v1.html` → `학교교육과정 편성` 규칙으로 자동 변환한다.
매니페스트에 제목을 따로 적으면 파일명과 제목 두 곳을 관리해야 해서 어긋날 여지가 생긴다.
색인이 끝나면 그 문서의 `.eyebrow`에서 Section 번호와 원문 쪽수를 읽어 버튼 아래에 덧붙인다.

## 결정 4. 검색 단위는 문서가 아니라 업무카드

세 문서 모두 `article.task` 구조가 완전히 같았다. `id="tN"`, `data-num`, `data-when`, `data-who`, `h3`, `.desc`, `.w6 dl`, `.memo`가 예외 없이 존재한다.
또 원본 css에 `.task { scroll-margin-top: var(--offset) }`가 있어 `#tN`으로 이동해도 상단 고정바에 카드가 가리지 않는다.
그래서 `파일.html#tN` 딥링크만으로 원하는 카드까지 바로 닿는다. 대상 문서를 고칠 필요가 없다.

앞으로 만들 문서도 이 구조를 유지해야 검색이 붙는다. 구조가 달라지면 `parseDoc` 함수를 손봐야 한다.

## 결정 5. 검색 순위 가중치

제목 10, 메뉴 경로 5, 태그·언제·누가 4, 본문 2로 두었다.
메뉴 경로에 높은 값을 준 이유는 `[성적-성적처리-...]` 같은 경로로 찾는 경우가 실제로 많을 것으로 봤기 때문이다.
쓰다 보고 순위가 어색하면 `score` 함수의 숫자만 바꾸면 된다.

## 결정 6. 스니펫은 낱말이 함께 보이는 구간을 고른다

처음에는 첫 번째로 맞은 낱말 위치를 잘라 썼는데, 두 낱말로 검색하면 화면에 한쪽만 보여 왜 걸렸는지 알 수 없었다.
지금은 각 낱말의 첫 위치를 후보로 두고 190자 창 안에 낱말이 가장 많이 들어오는 구간을 고른다.

## 결정 7. FAQ는 새 구조가 아니라 그룹 한 종류를 더한 것

FAQ를 위해 별도의 화면이나 별도의 검색 경로를 만들지 않았다.
매니페스트 그룹에 `tag`와 `id`만 선택 항목으로 더해서, `tag`가 있으면 배지가 'Part N' 대신 그 글자가 되고 앵커가 `id`가 되도록 했다.
문서 버튼·색인·검색·딥링크는 Part와 완전히 같은 길을 탄다. 나중에 '지침', '서식' 같은 묶음을 더 붙이더라도 매니페스트에 그룹 하나만 추가하면 된다.

이 과정에서 결함을 하나 잡았다. 배지 문자열을 화면 그리는 쪽에서만 만들고 색인 쪽에서는 `'Part ' + p.no`를 그대로 쓰고 있어서, FAQ 검색 결과의 출처가 `Part undefined`로 찍혔다.
`badgeOf()` 함수로 뽑아 두 곳이 같은 값을 쓰게 고쳤다.

## 결정 8. 파일 목록은 사람이 옮겨 적지 않는다

매니페스트를 화면 캡처만 보고 손으로 옮겨 적었더니 네 건이 실제 이름과 어긋났다.
`졸업_-_수료`가 실제로는 `졸업_·_수료`였고, `현장실습_관리`는 `현장실습생_관리`였다.
특히 창의적 체험활동 파일에 섞인 가운뎃점은 흔한 `·`(U+00B7)이 아니라 반각 `･`(U+FF65)이었다. 눈으로는 구분되지 않는다.

그래서 README의 파일 목록은 손으로 적지 않고 매니페스트와 실제 파일에서 뽑아 넣었다.
앞으로도 파일을 추가할 때는 이름을 타이핑하지 말고 복사해 붙이는 편이 안전하다.

## 결정 9. Part 표기는 매니페스트가 정본

51편의 eyebrow와 푸터 출처 표기가 제각각이었다. 같은 학적 문서인데 'Part 3. 학적', 'Part 2. 학적', 'Part. 학적', 그냥 '학적'이 섞여 있었고, 로마 숫자(Part Ⅰ, Ⅳ, Ⅸ)와 'Part 표기 없음'도 있었다.
`학급별 수상 관리`는 'Part 표기 없음', `행동특성 및 종합의견 관리`는 'Part 7. 학생생활'인데 둘 다 매니페스트에서는 Part 5다.

허브가 실제로 참조하는 것은 매니페스트이므로 그쪽을 정본으로 삼아 `Part N 라벨` 한 가지 형태로 51편 전부를 맞췄다.
아라비아 숫자를 쓴 이유는 허브 버튼의 배지가 'Part 3'이라서, 문서를 열었을 때 눈에 보이는 표기가 그대로 이어지게 하기 위해서다. 원본 매뉴얼의 로마 숫자는 버렸다.

한 파일 안에서 Part 표기가 나오는 자리는 두 곳이다. 상단 eyebrow와 하단 푸터의 출처 줄이다. 둘 다 고쳤다.
Section 번호와 쪽수는 원본 값을 그대로 두었다.

## 결정 10. 문서 정렬 기준은 Section 번호

Part 안에서 문서 순서가 파일명 순이라 뒤죽박죽이었다. 원본 매뉴얼의 Section 번호를 정렬 기준으로 삼았다.
번호는 파일명이 아니라 각 문서의 eyebrow에서 읽는다. 매니페스트에 번호를 따로 적어 두면 관리할 곳이 한 군데 늘고 어긋날 여지가 생기기 때문이다.
정렬해 놓고 보니 Part 3의 09와 11이 비어 있었고, 실제로 누락이었다. 위탁학생 관리(Section_09)와 공동교육과정 관리(Section_11)를 뒤에 채워 넣었다.
쪽수가 이어지는지로 확인할 수 있었다. 08이 pp. 362–389에서 끝나고 10이 pp. 422에서 시작하는데 그 사이 390–421이 비어 있었다.
Section 번호로 정렬해 두면 이런 구멍이 눈에 띈다는 점이 정렬의 부수 효과였다. 지금은 9개 Part 모두 빠진 번호가 없다.

주의할 점이 하나 있다. Part 3에는 이름이 비슷한 문서가 두 쌍 있다.
Section_09 `위탁학생 관리`와 Section_12 `타교수강생 관리(위탁)`, Section_11 `공동교육과정 관리`와 Section_13 `타교수강생 관리 (공동교육과정)`이다.
앞의 것은 우리 학교 학생을 내보내는 쪽, 뒤의 것은 다른 학교 학생을 받는 쪽이라 서로 다른 업무다. 검색하면 넷이 같이 걸리므로 헷갈리지 않게 볼 것.

## 결정 11. 문서에서 허브로 돌아가는 길을 낸다

53편을 전부 훑어보니 `pages/` 안에서 `index.html`로 가는 링크가 한 개도 없었다.
상단바의 `타임라인 ↑`은 `href="#timeline"`이라 문서 안에서만 움직이고, 바깥으로 나가는 링크는 나이스 광장과 폰트 CDN 둘뿐이었다.

이 사이트의 중심 기능은 검색인데, 교사의 실제 사용은 한 번 찾고 끝나지 않는다.
전입생이 오면 전입 관리를 보고 곧이어 수강과목 편성, 출결 관리로 이어진다. 그때 검색창으로 돌아갈 방법이 화면에 없었다.

두 자리에 냈다. 상단바의 `← 전체 색인`과, 문서 맨 아래 같은 Part 안의 앞뒤 문서로 가는 막대다.
앞뒤를 Part 안으로 가둔 이유는, 매니페스트 순서대로 죽 이으면 Part 3 학적의 마지막에서 Part 4 개별화교육계획으로 넘어가는 흐름이 뜻에 맞지 않기 때문이다.
Part에 문서가 한 편뿐인 1·4·9에서는 그 자리를 비워 둔다.

검색을 거쳐 들어온 경우에는 그냥 색인이 아니라 **그 검색 결과로** 돌아가야 한다.
그래서 허브가 결과 링크를 `pages/파일.html?q=..&who=..#tN` 으로 만들고, 문서 쪽 작은 스크립트가 그 물음표 뒷부분을 그대로 해시로 옮겨 복귀 링크에 붙인다.
`document.referrer`에는 조각(#) 이 실리지 않아서 그 길로는 안 된다.

## 결정 12. 필터 기준은 `data-who`이고, 화면에서만 표기를 합친다

검색이 사실상 필터 노릇을 못 하고 있었다. 실제로 재 보니 `학교생활기록부`가 196개 카드(전체의 31%), `출결` 168개, `마감` 159개였다.
`MAX_HITS`가 60이라 `학교생활기록부`를 찾은 사람은 196개 중 136개를 보지도 못한다.
화면 안내는 "낱말을 하나 더 넣어 범위를 좁혀 보세요"였는데, 이건 나이스 용어를 이미 아는 사람만 할 수 있는 일이다. 이 사이트가 가장 필요한 사람은 그 용어를 모르는 신규·전보 교사다.

622개 카드 전부에 `data-who`가 붙어 있는 것을 확인하고 그것을 기준으로 삼았다.
`w6`의 '누가?'는 문장이라 구두점 때문에 낱말로 자르기 불안정하다. `data-who`는 공백 구분이라 그대로 토큰이 된다.

역할이 36종인데 검색 하나에 14~26종이 걸린다. 다 늘어놓으면 결과보다 칩이 더 커진다.
그래서 개수 많은 순으로 8개만 보이고 나머지는 `+N개 더`로 접었다. 상위 8개면 어느 검색어에서든 주요 역할이 다 들어온다.
여러 개를 고르면 OR다. 담임이면서 교과담당인 경우가 흔하기 때문이다.

칩의 숫자는 **낱말로만 걸러낸 결과**에서 센다. 하나를 골랐다고 나머지 숫자가 무너지면 다음에 뭘 고를지 판단할 수가 없다.

표기가 갈린 역할이 넷 있었다. 담임교사(4)와 학급담임교사(142), 보건업무담당자(7)와 보건담당자(20), 학교업무분장관리자(2)와 학교업무분장관리담당자(19), 동아리업무담당교사(5)와 동아리담당교사(7)다.
같은 역할이 칩 두 개로 갈리면 필터가 망가지므로 합쳤다. 다만 원본 html은 건드리지 않고 허브의 `ROLE_ALIAS` 표에서만 합친다. 매뉴얼 원문 표기를 보존하기 위해서다.
성적처리담당교사(37)와 성적처리업무담당자(26), 위탁교육업무담당자(16)와 위탁과정업무담당자(10)는 서로 다른 사람을 가리킬 여지가 있어 합치지 않았다. 확인되면 표에 한 줄 더하면 된다.

효과는 이렇다. `마감` 159 → 담임 50, 교과 55. `승인요청` 101 → 담임 15. `학교생활기록부` 196 → 담임 67.

이 과정에서 결함을 하나 잡았다. 주소에 `&who=`를 달고 들어오면 역할이 곧바로 풀렸다.
색인이 끝나기 전에 복원 코드가 `runSearch`를 부르는데, 그 시점에는 색인이 비어 있어 "결과에 없는 역할은 자동 해제" 규칙에 걸려 버린 것이다.
색인이 다 돌기 전에는 그 판단을 하지 않도록 고쳤다.

## 결정 13. 문서 쪽 수정은 손이 아니라 스크립트로 한다

`pages/`의 53편은 다른 경로로 뽑아 온 산출물이다. 손으로 고쳐 두면 다시 뽑는 순간 사라진다.
그래서 `tools/patch_pages.py`로 만들었다. `class="docnav"`가 이미 있으면 건너뛰므로 몇 번을 돌려도 결과가 같다.
끼워 넣을 자리는 53편 전부에서 정확히 한 번씩만 나오는 문자열 다섯 개로 잡았고, 하나라도 개수가 어긋나면 멈추고 파일 이름을 알려 준다.

새 문서를 `pages/`에 넣으면 이 스크립트를 한 번 돌려야 복귀 링크가 붙는다. README에도 적어 두었다.

## 안 넣은 것

- 시기별 필터. `data-when`이 189종으로 흩어져 있어 학년초·학기초·학기중·고사전후·학기말·학년말·수시 정도로 정규화하는 손질이 먼저 필요하다.
- 검색어 없이 역할만으로 훑기. 지금 역할 칩은 검색 결과를 좁히는 용도로만 쓴다.
- localStorage 색인 캐시. 문서가 자주 바뀌는 동안에는 오히려 옛 내용이 남는다.
- 검색 결과 새 탭 열기. 같은 탭에서 열되 검색어를 주소 해시에 남겨 뒤로가기로 돌아오게 했다.

## 알려진 제약

- `file://`로 직접 열면 브라우저가 다른 파일 읽기를 막아 검색이 동작하지 않는다. 안내 문구를 띄워 두었다.
- 파일 이름이 한글이라 주소에서는 인코딩되어 보인다. 동작에는 문제가 없지만, 배포 후 특정 문서만 404가 나면 그 파일 이름의 특수문자를 의심하면 된다.
